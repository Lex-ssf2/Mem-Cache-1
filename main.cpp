#include <iostream>
#include <cmath>
#include <vector> 
#include "cache.hpp"
  
using namespace std;

int main(int argc, const char* argv[])
{
    int totalBloque = 0;
    int tamanoBloque = 0;
    int offsetPalabra = log2(offsetPalabra);
    int offsetBit = log2(tamanoBloque);
    /*
        Palabra =
    */
    int etiqueta = 0 >> offsetBit;
    int indice =  0;
    return 0;
}